# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — ห้าม commit ข้อมูลลับ
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# ไฟล์อัปโหลด (โลโก้โรงเรียน, รูปนักเรียน) — ไม่ commit ลง git (เก็บบนดิสก์/volume เท่านั้น)
/uploads
# ไฟล์ backup ฐานข้อมูล (pg_dump) — เก็บนอก git
/backups
*.sql
*.dump
